# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Category {
  id: ID!
  name: String!
  products: [Product]
}

input CreateOrderInput {
  items: [OrderItemInput!]!
  status: String = "pending"
  total: Float!
  userId: Float!
}

input CreateProductInput {
  categoryId: Float!
  description: String
  image: String
  name: String!
  price: Float!
}

input CreateReviewInput {
  comment: String
  productId: Float!
  rating: Int!
  userId: Float!
}

type Mutation {
  createOrder(data: CreateOrderInput!): Order!
  createProduct(data: CreateProductInput!): Product!
  createReview(data: CreateReviewInput!): Review!
  deleteOrder(id: Int!): Order!
  deleteProduct(id: Int!): Product!
  deleteReview(id: Int!): Review!
  updateOrder(data: UpdateOrderInput!): Order!
  updateProduct(data: UpdateProductInput!): Product!
  updateReview(data: UpdateReviewInput!): Review!
}

type Order {
  id: ID!
  items: [OrderItem!]!
  status: String!
  total: Float!
  user: User!
}

type OrderItem {
  id: ID!
  price: Float!
  product: Product!
  quantity: Float!
}

input OrderItemInput {
  price: Float!
  productId: Float!
  quantity: Float!
}

type OrderTypeConnection {
  hasNextPage: Boolean
  hasPreviousPage: Boolean
  items: [Order]
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

type Product {
  category: Category
  description: String
  id: ID!
  image: String
  name: String!
  price: Float!
  reviews: [Review]
}

type ProductTypeConnection {
  hasNextPage: Boolean
  hasPreviousPage: Boolean
  items: [Product]
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

type Query {
  order(id: Int!): Order!
  orders(limit: Int = 10, page: Int = 1): OrderTypeConnection!
  product(id: Int!): Product!
  productReviews(productId: Int!): [Review!]!
  products(limit: Int = 10, page: Int = 1): ProductTypeConnection!
  review(id: Int!): Review!
  reviews(limit: Int = 10, page: Int = 1): ReviewTypeConnection!
  userOrders(userId: Int!): [Order!]!
  userReviews(userId: Int!): [Review!]!
}

type Review {
  comment: String
  id: ID!
  product: Product!
  rating: Float!
  user: User!
}

type ReviewTypeConnection {
  hasNextPage: Boolean
  hasPreviousPage: Boolean
  items: [Review]
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

input UpdateOrderInput {
  id: ID!
}

input UpdateProductInput {
  id: ID!
}

input UpdateReviewInput {
  id: ID!
}

type User {
  email: String!
  id: ID!
  name: String
  orders: [Order]
  reviews: [Review]
}